List集合概述

1.有序集合，用户可以精确控制列表中每个元素的插入位置，用户可以通过整数索引访问元素，
并搜索列表中的元素
2.与Set集合不同，列表通常允许重复元素

List集合特点

1.有序:存储和取出的元素顺序一致
2.可重复:存储的元素可以重复

List特有方法

1.add(int index,E element);
2.remove(int index);
3.set(int index,E element);
4.get(int index);